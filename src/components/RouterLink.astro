---
// Types
type Props = {
  to: string;
  [k: string]: any;
};

// Injects
const { to, ...rest } = Astro.props;
---

<a
  href={import.meta.env.BASE_URL === '/' ? to : import.meta.env.BASE_URL + to}
  {...rest}>
  <slot />
</a>
