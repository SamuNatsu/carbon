---
// Types
type Props = {
  href: string;
  [k: string]: any;
};

// Injects
const { href, ...rest } = Astro.props;
---

<a
  href={import.meta.env.BASE_URL === '/'
    ? href
    : import.meta.env.BASE_URL + href}
  {...rest}>
  <slot />
</a>
